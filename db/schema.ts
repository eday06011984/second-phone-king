import {sqliteTable,text,integer,index} from "drizzle-orm/sqlite-core";
export const stores=sqliteTable("stores",{id:text("id").primaryKey(),owner:text("owner").notNull().unique(),name:text("name").notNull(),city:text("city").notNull(),address:text("address").notNull(),phone:text("phone").notNull(),line:text("line").notNull().default(""),created:text("created").notNull()});
export const listings=sqliteTable("listings",{id:text("id").primaryKey(),storeId:text("store_id").notNull().references(()=>stores.id),brand:text("brand").notNull(),model:text("model").notNull(),storage:text("storage").notNull(),color:text("color").notNull(),price:integer("price").notNull(),battery:integer("battery"),condition:text("condition").notNull(),warranty:text("warranty").notNull(),description:text("description").notNull(),images:text("images").notNull(),status:text("status").notNull().default("active"),created:text("created").notNull(),updated:text("updated").notNull()},t=>[index("idx_listings_store").on(t.storeId),index("idx_listings_status_brand").on(t.status,t.brand)]);
